---
phase: 08-code-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - MarketSense-backend/app/router.py
  - MarketSense-backend/app/routes/data_route.py
  - MarketSense-backend/app/routes/models.py
  - MarketSense-backend/app/services/model_trainer.py
  - MarketSense-backend/app/services/model_predictor.py
  - Marketsense-frontend/pages/3_Model_Management.py
  - Marketsense-frontend/app.py
  - Marketsense-frontend/pages/5_About.py
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03

must_haves:
  truths:
    - "Unused /data route removed from backend (never called from frontend)"
    - "Unused /models route removed from backend (never called from frontend)"
    - "Linear Regression training code removed from backend (never used)"
    - "Linear Regression option removed from frontend UI (cannot be trained)"
    - "Prophet-only support in training - clear and consistent"
  artifacts:
    - path: "MarketSense-backend/app/router.py"
      provides: "Updated router - removed data_router and models_router, kept evaluate_router"
    - path: "MarketSense-backend/app/routes/data_route.py"
      provides: "File deleted - contained unused /data route"
    - path: "MarketSense-backend/app/routes/models.py"
      provides: "File deleted - contained unused /models route"
    - path: "MarketSense-backend/app/services/model_trainer.py"
      provides: "File deleted - contained unused train_linear_model()"
    - path: "MarketSense-backend/app/services/model_predictor.py"
      provides: "File deleted - contained unused predict_linear_model()"
    - path: "Marketsense-frontend/pages/3_Model_Management.py"
      provides: "Updated - removed Linear Regression from dropdown"
    - path: "Marketsense-frontend/app.py"
      provides: "Updated - removed Linear Regression mention"
    - path: "Marketsense-frontend/pages/5_About.py"
      provides: "Updated - removed Linear Regression mention"
  key_links:
    - from: "Frontend"
      to: "Backend"
      via: "All required endpoints still work: /health, /fetch-data, /predict, /models/list, /models/get-all, /train, /evaluate"
---

<objective>
Remove unused backend routes and non-functional Linear Regression model code from the codebase.

Purpose: Clean up dead code to improve maintainability and remove misleading options that don't work.

CRITICAL: Must NOT break any existing functionality. All used endpoints must remain functional.

Output: Deleted unused files, updated router, removed Linear Regression training (but kept evaluation support).
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-code-cleanup/08-CONTEXT.md

## Verified Frontend API Usage (MUST KEEP WORKING)

| Endpoint | Used By | Status |
|----------|---------|--------|
| `/health` | app.py | ✓ KEEP |
| `/fetch-data` | dashboard_service.py | ✓ KEEP |
| `/predict` | dashboard_service.py, api_client.py, components/api_client.py | ✓ KEEP |
| `/models/list` | model_service.py | ✓ KEEP |
| `/models/get-all` | model_service.py | ✓ KEEP |
| `/train` | model_service.py, api_client.py | ✓ KEEP |
| `/evaluate` | 2_Model_Performance.py | ✓ KEEP |

## WHAT TO DELETE (never called)

1. **data_route.py** - contains `/data` endpoint, NEVER called (frontend uses `/fetch-data`)
2. **models.py** - contains `/models` endpoint, NEVER called (frontend uses `/models/list`)
3. **model_trainer.py** - contains `train_linear_model()`, NEVER imported anywhere
4. **model_predictor.py** - contains `predict_linear_model()`, NEVER imported anywhere

## WHAT TO UPDATE

**router.py:**
- Remove: `from app.routes.data_route import data_router`
- Remove: `from app.routes.models import router as models_router`
- Remove: `appRouter(data_router)`
- Remove: `appRouter(models_router)`
- KEEP: `appRouter(evaluate_router)` (used!)

**Frontend model dropdown:**
- Change from `["Prophet", "Linear Regression"]` to `["Prophet"]`

## WHAT TO KEEP (evaluation support)

- evaluation_service.py - supports BOTH Prophet and Linear Regression evaluation
- This allows evaluating any existing model files on disk
- Training is Prophet-only (which is what users can actually do)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused backend routes and Linear Regression code</name>
  <files>
    - MarketSense-backend/app/router.py
    - MarketSense-backend/app/routes/data_route.py
    - MarketSense-backend/app/routes/models.py
    - MarketSense-backend/app/services/model_trainer.py
    - MarketSense-backend/app/services/model_predictor.py
    - Marketsense-frontend/pages/3_Model_Management.py
    - Marketsense-frontend/app.py
    - Marketsense-frontend/pages/5_About.py
  </files>
  <action>
## PRECISE STEP-BY-STEP EXECUTION

### STEP 1: Update router.py (CRITICAL - keep evaluate_router!)

Edit `MarketSense-backend/app/router.py`:
```python
# BEFORE:
from app.routes.data_route import data_router
from app.routes.evaluate import router as evaluate_router
from app.routes.models import router as models_router

def router(appRouter):
    appRouter(data_router)      # REMOVE
    appRouter(models_router)    # REMOVE
    appRouter(evaluate_router) # KEEP

# AFTER:
from app.routes.evaluate import router as evaluate_router

def router(appRouter):
    appRouter(evaluate_router)  # KEEP - used by frontend!
```

### STEP 2: DELETE unused route files

Delete these files (they contain routes never called from frontend):
- `MarketSense-backend/app/routes/data_route.py` - DELETE (contains `/data`)
- `MarketSense-backend/app/routes/models.py` - DELETE (contains `/models`)

### STEP 3: DELETE unused Linear Regression training files

Delete these files (they are NEVER imported anywhere in the codebase):
- `MarketSense-backend/app/services/model_trainer.py` - DELETE
- `MarketSense-backend/app/services/model_predictor.py` - DELETE

### STEP 4: UPDATE frontend - Remove Linear Regression option

A. Edit `Marketsense-frontend/pages/3_Model_Management.py`:
   - Find line: `st.selectbox("Select Model", ["Prophet", "Linear Regression"], index=0)`
   - Change to: `st.selectbox("Select Model", ["Prophet"], index=0)`

B. Edit `Marketsense-frontend/app.py`:
   - Find mention of "Linear Regression" in hero text
   - Change to only mention "Prophet"

C. Edit `Marketsense-frontend/pages/5_About.py`:
   - Remove all mentions of "Linear Regression"
   - Update text to say "Prophet" only

### VERIFICATION COMMANDS (run after changes):

```bash
# Should return 0 results - /data never called from frontend
grep -r "/data" Marketsense-frontend/ --include="*.py" | wc -l

# Should return 0 results - LinearRegression training removed  
grep -r "LinearRegression\|linear_regression\|train_linear_model" MarketSense-backend/app/ --include="*.py" | wc -l

# Should return 1 result - evaluate router still present
grep -r "evaluate_router" MarketSense-backend/app/router.py | wc -l
```
  </action>
  <verify>
    <automated>
# After changes, verify:
# 1. No /data calls from frontend
grep -r "/data" Marketsense-frontend/ --include="*.py" | grep -v "fetch-data" | wc -l
# 2. No LinearRegression training code
grep -r "LinearRegression\|train_linear_model" MarketSense-backend/app/ --include="*.py" | wc -l
# 3. Evaluate router still present
grep "evaluate_router" MarketSense-backend/app/router.py | wc -l
    </automated>
  </verify>
  <done>
    - router.py updated - data_router and models_router removed, evaluate_router kept
    - data_route.py deleted (unused /data endpoint)
    - models.py deleted (unused /models endpoint)
    - model_trainer.py deleted (unused training code)
    - model_predictor.py deleted (unused prediction code)
    - Frontend dropdown only shows "Prophet"
    - All Linear Regression text removed from frontend
    - All used endpoints (/health, /fetch-data, /predict, /models/list, /models/get-all, /train, /evaluate) still work
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] /data route no longer exists in backend
- [ ] /models route no longer exists in backend  
- [ ] /evaluate route still works (was kept)
- [ ] No LinearRegression imports in backend services
- [ ] Frontend dropdown only shows Prophet
- [ ] All used API endpoints still work (tested end-to-end)

**CRITICAL:** After changes, verify the app works end-to-end:
1. Start backend: uvicorn app.main:app
2. Start frontend: streamlit run app.py
3. Test: Dashboard loads data, Model Training works, Model Performance shows metrics
</verification>

<success_criteria>
Measurable completion:
- grep for "/data" in frontend returns 0 (only /fetch-data used)
- grep for "LinearRegression" in backend returns 0
- grep for "evaluate_router" in router.py returns 1 (kept)
- Model dropdown in Model Management only shows "Prophet"
- End-to-end test passes: train Prophet model, view predictions, view metrics
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-cleanup/08-01-SUMMARY.md`
</output>
