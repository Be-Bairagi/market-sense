---
phase: 08-code-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - MarketSense-backend/tests/test_routes/test_data.py
  - MarketSense-backend/ReadMe.md
  - Marketsense-frontend/pages/2_Model_Performance.py
autonomous: true
requirements:
  - CLN-04
  - CLN-05

must_haves:
  truths:
    - "Test file for deleted /data route removed"
    - "Documentation updated to remove /data references"
    - "Remaining Linear Regression mentions removed from frontend"
---

<objective>
Fix gaps found during UAT - remove orphaned tests and documentation references to deleted endpoints.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix orphaned tests and documentation</name>
  <files>
    - MarketSense-backend/tests/test_routes/test_data.py
    - MarketSense-backend/ReadMe.md
    - Marketsense-frontend/pages/2_Model_Performance.py
  </files>
  <action>
## STEP 1: Delete orphaned test file

The test file tests the deleted `/data` endpoint which no longer exists.

Delete: `MarketSense-backend/tests/test_routes/test_data.py`

## STEP 2: Update documentation

Edit `MarketSense-backend/ReadMe.md`:
- Remove or update references to `/data` endpoint
- Keep only `/fetch-data` which is the active endpoint

## STEP 3: Remove Linear Regression mention

Edit `Marketsense-frontend/pages/2_Model_Performance.py` line 207:
- Remove: "Compare Linear Regression vs LSTM performance."
- Or change to: "Compare different Prophet model configurations."
  </action>
  <verify>
    <automated>
# Verify:
# 1. No test file for deleted endpoint
ls MarketSense-backend/tests/test_routes/test_data.py 2>&1 | grep -q "No such file" && echo "PASS" || echo "FAIL"
# 2. No /data in ReadMe
grep -q "/data" MarketSense-backend/ReadMe.md && echo "FAIL" || echo "PASS"
# 3. No Linear Regression in frontend
grep -q "Linear Regression" Marketsense-frontend/pages/2_Model_Performance.py && echo "FAIL" || echo "PASS"
    </automated>
  </verify>
  <done>
    - test_data.py deleted
    - ReadMe.md updated
    - 2_Model_Performance.py updated
  </done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/08-code-cleanup/08-02-SUMMARY.md`
