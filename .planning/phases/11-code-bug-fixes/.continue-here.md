---
phase: 11-code-bug-fixes
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-03-02T21:52:34.716Z
---

<current_state>
Phase 11 (Code Bug Fixes) is planned but not yet executed. The plan was created and we started working on Task 3 (fixing the frontend path).
</current_state>

<completed_work>

- Created Phase 11 plan with 4 bug fix tasks
- Task 3: Fixed hardcoded Windows backslash path in frontend app.py - DONE

</completed_work>

<remaining_work>

- Task 1: Fix HTTPException in prediction_service.py (lines 24, 47 - change positional to keyword args)
- Task 2: Verify HTTPException in data_fetcher.py (may already be correct)
- Task 4: Remove duplicate data_fetcher.py (check if unused, remove if so)

</remaining_work>

<decisions_made>

- Created Phase 11 specifically to address bugs found during code review
- Fixed path immediately when error was reported by user
- Prioritized BUG-03 as it was causing immediate crash

</decisions_made>

<blockers>

- None currently - work can continue

</blockers>

<context>
We identified bugs during a code review of the MarketSense project. The bugs include:
1. HTTPException using positional args instead of keyword args in prediction_service.py
2. Hardcoded Windows backslash in frontend path (fixed)
3. Duplicate code in data_fetcher.py vs fetch_data_service.py

Task 3 was just completed - the Windows path was causing the Streamlit app to crash on Linux.

</context>

<next_action>
Start with Task 1: Fix HTTPException in prediction_service.py - change:
- `raise HTTPException(400, "n_days must be positive")` 
- to `raise HTTPException(status_code=400, detail="n_days must be positive")`

And similar changes for line 47.

Run: /gsd-execute-phase 11 to continue
</next_action>
